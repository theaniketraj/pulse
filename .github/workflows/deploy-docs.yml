name: Deploy Documentation

on:
  push:
    branches:
      - main
    paths:
      - 'Docs/**'
      - 'package.json'
      - 'package-lock.json'
      - '.github/workflows/deploy-docs.yml'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Fix case sensitivity for markdown files
        run: |
          cd Docs
          
          echo "=== Files before rename ==="
          ls -la *.md
          
          # Rename all uppercase .md files to lowercase
          for file in *.md; do
            if [ -f "$file" ]; then
              lowercase=$(echo "$file" | tr '[:upper:]' '[:lower:]')
              if [ "$file" != "$lowercase" ]; then
                mv "$file" "$lowercase"
                echo "Renamed $file to $lowercase"
              fi
            fi
          done
          
          echo "=== Files after rename ==="
          ls -la *.md
          
          echo "=== Checking for uppercase links in quickstart.md ==="
          grep -n "SYSTEM_ARCHITECTURE\|DEVELOPMENT\|API\|COMPONENTS\|GETTING_STARTED" quickstart.md || echo "No uppercase links found"
          
          # Fix internal links to use lowercase filenames
          echo "=== Fixing links ==="
          for file in *.md; do
            if [ -f "$file" ]; then
              # Replace uppercase .md references with lowercase
              # Format: [TEXT](SYSTEM_ARCHITECTURE.md) -> [TEXT](system_architecture.md)
              perl -i -pe 's/SYSTEM_ARCHITECTURE\.md/system_architecture.md/g' "$file"
              perl -i -pe 's/DEVELOPMENT\.md/development.md/g' "$file"
              perl -i -pe 's/API\.md/api.md/g' "$file"
              perl -i -pe 's/COMPONENTS\.md/components.md/g' "$file"
              perl -i -pe 's/GETTING_STARTED\.md/getting_started.md/g' "$file"
              perl -i -pe 's/QUICKSTART\.md/quickstart.md/g' "$file"
              perl -i -pe 's/INTRODUCTION\.md/introduction.md/g' "$file"
              perl -i -pe 's/CONTRIBUTING\.md/contributing.md/g' "$file"
              perl -i -pe 's/TESTING\.md/testing.md/g' "$file"
              perl -i -pe 's/TROUBLESHOOTING\.md/troubleshooting.md/g' "$file"
              perl -i -pe 's/VISION\.md/vision.md/g' "$file"
              perl -i -pe 's/PROJECT_STRUCTURE\.md/project_structure.md/g' "$file"
              perl -i -pe 's/CODE_OF_CONDUCT\.md/code_of_conduct.md/g' "$file"
              perl -i -pe 's/SECURITY\.md/security.md/g' "$file"
              echo "Fixed links in $file"
            fi
          done
          
          echo "=== Checking for uppercase links in quickstart.md after fix ==="
          grep -n "SYSTEM_ARCHITECTURE\|DEVELOPMENT\|API\|COMPONENTS\|GETTING_STARTED" quickstart.md || echo "No uppercase links found"
          
          cd ..

      - name: Build documentation
        run: npx vitepress build Docs

      - name: Fix index.html filename (GitHub Pages expects lowercase)
        run: |
          if [ -f "Docs/.vitepress/dist/INDEX.html" ]; then
            mv Docs/.vitepress/dist/INDEX.html Docs/.vitepress/dist/index.html
            echo "Renamed INDEX.html to index.html"
          fi

      - name: Debug - Show build output structure
        run: |
          echo "=== Build output directory structure ==="
          ls -la Docs/.vitepress/dist/
          echo ""
          echo "=== First 30 lines of index.html ==="
          head -30 Docs/.vitepress/dist/index.html
          echo ""
          echo "=== Asset paths in index.html ==="
          grep -o 'href="[^"]*"' Docs/.vitepress/dist/index.html | head -10
          grep -o 'src="[^"]*"' Docs/.vitepress/dist/index.html | head -10
          echo ""
          echo "=== Check for critical files ==="
          ls -la Docs/.vitepress/dist/index.html || echo "index.html NOT FOUND"
          ls -la Docs/.vitepress/dist/vp-icons.css || echo "vp-icons.css NOT FOUND"
          ls -la Docs/.vitepress/dist/assets/ || echo "assets/ NOT FOUND"
          echo ""
          echo "=== VitePress config base setting ==="
          grep -A 2 "base:" Docs/.vitepress/config.mts || echo "No base setting found"

      - name: Deploy to GitHub Pages
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: Docs/.vitepress/dist
          cname: false

      - name: Debug - Show deployment summary
        run: |
          echo "=== Deployment completed ==="
          echo "Expected URL: https://theaniketraj.github.io/pulse/"
          echo "Files deployed from: Docs/.vitepress/dist"
          echo "Target branch: gh-pages"